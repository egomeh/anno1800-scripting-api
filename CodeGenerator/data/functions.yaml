
- functions:

    - GetGameTime:
        - out:
            - time: uint64
            - frame: uint64

    - GetShipWaypoints:
        - in:
            - shipID: uint64
        - out:
            - waypoints: list(Coordinate)

    - GetIslandIds:
        - out:
            - ids: list(uint64)

- types:

    - Coordinate:
        - fields:
            - x: float
            - y: float

    - ShipCargo:
        - fields:
            - cargo: list(CargoSlot)

    - CargoSlot:
        - fields:
            - item: uint32
            - volume: int32

    - SomeListyBoi:
        - fields:
            - ids: list(uint64)
