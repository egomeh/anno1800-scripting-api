


- functions:

    - GetGameTime:
        - out:
            - time: uint64

    - GetShipWaypoints:
        - in:
            - shipID: uint64
        - out:
            - waypoints: list(Coordinate)


- types:

    - Coordinate:
        - fields:
            - x: float
            - y: float

